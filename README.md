# vi — Project scaffold

This is a minimal Python project scaffold created automatically.

Files included:
- `src/main.py` — small example entrypoint
- `tests/test_main.py` — tiny pytest test
- `requirements.txt` — dev deps

Quick start (PowerShell):
```powershell
python -m pip install -r .\requirements.txt
python -m pytest -q
```
